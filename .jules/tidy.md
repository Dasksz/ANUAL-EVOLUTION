## 2025/06/05 : (Abstracting Chart Render Logic for specific layout) **Aprendizado:** (Added chart logic using Chart.js inside a dedicated container avoiding modifying KPI rendering functions.) **Ação:** (Instead of appending canvas directly to KPIs block, created a dedicated div `lpEvolutionChartContainer` and used `Chart.js` directly within a scoped `renderLpEvolutionChart`.)
## 2024-05-19 : (Correct Progress Bar Calcs in Estrelas View) **Aprendizado:** Progress bars in the Estrelas dashboard for 'Positivação' and 'Aceleradores' were previously calculated against the entire client base (`data.base_clientes`) instead of the specific targets for those metrics (`metaPos` and `metaAcel`). This caused the displayed percentages to be much lower than the actual performance against the goals. **Ação:** Always ensure the denominator in KPI percentage calculations matches the specific target (meta) for that KPI, not a general metric like the overall client base. Use fallbacks for division-by-zero when the meta is 0 but there is realized data.
## 2026-06-22 - Fix n8n LangChain Tool Invocation
**Aprendizado:** Modelos de raciocínio profundo (DeepSeek R1/thinking mode) apresentam problemas de `reasoning_content` não propagado corretamente no fluxo LangChain do n8n ao realizar chamadas em cadeia para ferramentas.
**Ação:** Mudar para o modelo `deepseek-chat` (DeepSeek V3) evita o erro mantendo alta qualidade de resposta. Além disso, quando o back-end (Supabase) consolida múltiplas visões numa única RPC (`n8n_agent_view_v2`), o System Prompt deve ser reescrito rigorosamente para suprimir referências aos nomes antigos de ferramentas, forçando o Agente a passar as flags (como `opcao=1`) para a única ferramenta consolidada.
## 2024/06/23 : (Add Ramo Atividade Column to Uploader) **Aprendizado:** (Added `ramo_atividade` column mapping to database and uploader logic dynamically checking input data without disrupting existing `ramo` data) **Ação:** (Extracted data cleanly utilizing existing logic checks for upper casing and null coalescing before persisting to data_clients.)
## 2026/59M/06/25/26D : (Fix ID mismatch for year filter) **Aprendizado:** (DOM queries that fail silently, like `document.getElementById` returning `null` in early returns, can cause UI components to indefinitely show a loading state without console errors. Always verify HTML IDs match JS queries.) **Ação:** (When resolving stuck loading states with no errors, check the DOM query logic for null returns before investigating network issues.)
## 2024-05-18 : (UX City Filter in SQL) **Aprendizado:** Filtering display sets in analytical dashboards requires dynamically pruning the baseline data (like available cities) rather than applying blanket mathematical zeros. Applying the zero values leaves "ghost" rows that clutter the UI when filtering specific datasets. By creating a custom dynamic WHERE clause strictly for baseline generation independent of metrics timing constraints, the correct subsets display cleanly. **Ação:** Used explicit dynamic CTEs inside plpgsql based on generic active filter states (`v_has_filters`) to subset `config_city_branches` dynamically before LEFT JOINs.
## 2026/06/25 : (Fix ghost rows and missing total in Share dashboard) **Aprendizado:** (When applying frontend-driven database filtering, ensure the baseline data table (e.g. city lists) is also filtered alongside the operational data. Otherwise, unfiltered baseline rows will appear with zeroed values. Also, when modifying table logic, ensure aggregation functions correctly output all needed summary values to the frontend.) **Ação:** (Added specific `v_where_cb` to the `config_city_branches` join to filter the baseline. Added `totalPop` calculation in the frontend map loop to populate missing HTML field.)
## 2024-06-26 : Fix layout leakage for table
**Aprendizado:** A stray closing `</div>` tag can prematurely close a view container (like `#city-view`), causing subsequent sibling elements (like the "Performance - Segmentação" table) to leak out and render globally across other views within the parent layout.
**Ação:** Always verify DOM nesting mathematically (`count(<div) == count(</div>)`) within specific component boundaries when elements appear on unintended pages.
## 2026-06-26 - Fix sp_mix_ideal_cliente
## 2024-05-24 : (Utilize setupDefaultMultiSelect and resetDateDropdowns) **Aprendizado:** While extracting repetitive code is beneficial, extracting wrapper logic to globals inside a monolithic file (`setupDefaultMultiSelect`) causes fatal execution errors if the new helper definition is mistakenly omitted or not hoisted properly. Always verify definition presence before calling. **Ação:** Define globals comprehensively and verify via basic node execution (`node -c`) before submitting.
## 2026-06-27 : (Refactor Loja Perfeita processing) **Aprendizado:** Shifting heavy data aggregation loops out of JavaScript web workers and into PostgreSQL using SQL native GROUP BY capabilities reduces client-side processing bottlenecks and enables dynamic filtering queries to return grouped views of real-time data more predictably. **Ação:** Offload data grouping to SQL queries rather than aggregating logic during frontend CSV/Excel uploading processes when dynamic metrics calculations are required.
## 2026/07/04 : (Fixing dynamic SQL single quotes) **Aprendizado:** Quando houver consultas SQL dentro de blocos dinâmicos como `EXECUTE format('...')` no PostgreSQL, as aspas simples internas da string literal (ex: `IN ('5', '11')`) precisam ser escapadas dobrando-as (ex: `IN (''5'', ''11'')`) para não causar erro de sintaxe 42601 e quebrar a execução do format. Além disso, ao consultar dados brutos que usam `vlbonific` em oposição à tabela sumarizada que usa `bonificacao`, é crucial manter a consistência de colunas para evitar o erro de 'column does not exist'. **Ação:** As aspas simples em `get_boxes_dashboard_data` foram escapadas em todo o bloco `EXECUTE format` e a coluna `bonificacao` foi alterada para `vlbonific` dentro de `prod_agg`.
## 2026/07/04 : (Fixing Timeout in prod_base) **Aprendizado:** No FAST PATH do dashboard de caixas, a construção da CTE `prod_base` lia a tabela gigante `data_history` usando os parâmetros `v_previous_year, v_current_year` para a cláusula `dtped >= make_date(%L, 1, 1) AND EXTRACT(YEAR FROM dtped) = %L`. Isso fazia o banco buscar desde 2023 mas filtrar por 2024, ignorando a otimização de índice e causando Statement Timeout (>15s). **Ação:** O parâmetro foi corrigido para `v_current_year, v_current_year` para que a busca em `data_history` seja filtrada eficientemente apenas para o ano corrente.
