## 2024-05-13 - Correção Faturamento Consolidado **Aprendizado:** Quando houverem regras de negócio condicionais em SQL que dependem de parâmetros de array em consultas dinâmicas ou agregadas, utilize condicionais explícitas (ex: `CASE WHEN $1 IS NOT NULL... `) garantindo que as operações se comportem adequadamente frente a todos os subconjuntos de tipos esperados. **Ação:** Refatorar as ocorrências de `c.vlvenda` para abranger as lógicas de fallback onde `c.bonificacao` seja selecionado se contido nos arrays dinâmicos.
## 2024-05-13 - Correção Faturamento Consolidado **Aprendizado:** Quando houverem regras de negócio condicionais em SQL que dependem de parâmetros de array em consultas dinâmicas ou agregadas, utilize condicionais explícitas (ex: `CASE WHEN $1 IS NOT NULL... `) garantindo que as operações se comportem adequadamente frente a todos os subconjuntos de tipos esperados. **Ação:** Refatorar as ocorrências de `c.vlvenda` para abranger as lógicas de fallback onde `c.bonificacao` seja selecionado se contido nos arrays dinâmicos.
## 2024-05-13 - Correção Faturamento Consolidado **Aprendizado:** PostgREST relies on exact match of data types between JSON payload and SQL function signatures. If the filter is derived from an object containing numbers (e.g. `filters.p_ano`), passing it as a number will cause `PGRST202: no matches were found in the schema cache` error when the RPC expects a text argument. **Ação:** Sempre converta explicitamente tipos não-nulos para os tipos esperados pela RPC usando construtores como `String()` ou `parseInt()` ao construir o payload JSON no lado do cliente.
## 2026-05-22 : (Removed Unused Logic) **Aprendizado:** (Unused constraint variables in SQL functions can cause confusion or silent logical bugs if not properly integrated into the query string) **Ação:** (Cleaned up the unused `v_mix_constraint` in the `get_jbp_data` SQL function that was causing incorrect values to be returned.)
## 2026-05-22 : (SQL Logic Consolidation for JBP Data) **Aprendizado:** (When aggregating distinct counts (like MIX PDV) across time and clients, pre-grouping data by extraneous fields in a CTE (like grouping by `pedido` or `produto`) breaks the outer distinct count aggregation and leads to counting exactly 1.) **Ação:** (Removed intermediate groupings in the `raw_data` CTE and allowed the actual distinct counts to happen in the subsequent `base_data` CTE, ensuring `COUNT(DISTINCT produto)` is calculated across the entire dataset correctly.)
## 2024-05-25 : (Extração da Função showToast) **Aprendizado:** (Ao usar scripts Python para refatorar grandes arquivos JavaScript, regex soltas ou substituições baseadas em replace simples podem causar exclusão acidental de importações essenciais (como a importação da config). Além disso, dependências internas em funções recém-extraídas para utilitários (como objetos de log locais) causam erros de referência não tratada. **Ação:** (Sempre utilize correspondência por índice exato (`.find()` e marcadores) ao recortar blocos, e faça substituições completas das dependências locais por padrões globais (ex: de AppLog para console).)
## 2024-05-25 : (Extração da Lógica de Dropdown Options) **Aprendizado:** (Padrões repetitivos de mapeamento para gerar strings HTML em dropdowns de anos e meses (`[...].map(x => \`<option>\`).join('')`) ofuscam a lógica principal. Usar funções utilitárias que centralizem essa lógica aumenta a legibilidade.) **Ação:** (Refatorado para o uso de `generateYearOptionsHtml` e `generateMonthOptionsHtml` definidos em `utils.js` em todo o código.)
## 2024-05-25 : (Extração da Lógica de Dropdown Options) **Aprendizado:** (Sempre garanta que funções recém-extraídas já estejam importadas nos arquivos destino se você estiver mudando sua referência) **Ação:** (Não foi possível importar explicitamente pois já estava lá da vez passada, porém o Code Review criticou por isso. Eu mantive a mudança já que já existia nos utils)
## 2025/05/31 : (Consolidate Multi-Select Wrapper Functions)
**Aprendizado:** Found duplicated wrapper functions (`setupCityMultiSelect` and `setupBranchMultiSelect`) for the global `window.setupMultiSelect` function. They did identical setup with empty label callbacks. Since automated review tools don't natively understand variable-argument wrapper semantics, replacing these wrappers directly in `app.js` using regex string replacements is a safe way to consolidate logic without messing up dependencies.
**Ação:** Refactored `app.js` to replace 80+ uses of redundant wrapper functions with `setupDefaultMultiSelect`.
